# Local free fallback: use Ollama without a paid API key.
# For production, replace these values with your own provider keys.

OPENAI_API_KEY = "ollama"
OPENAI_BASE_URL = "http://localhost:11434/v1"
OPENAI_MODEL = "llama3.1"
AI_TEST_GEN_MODE = "live"
SANDBOX_TYPE = "docker"
